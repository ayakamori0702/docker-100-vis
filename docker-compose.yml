version: '3'
services: 
    anaconda:
        build: .
        volumes:
            - '.:/opt/100knock-process-visualization'
        ports:
            - '9999:9999'
        tty: true
        platform: linux/amd64